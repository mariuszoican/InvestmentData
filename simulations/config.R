# config.R

config <- list(
  mu        = 0.15,
  sigma     = 0.12,
  sigma_imb = 0.08,
  w0        = 100,
  gamma     = 24,
  N_plot.   = 16,
  # defaults for simulation
  N         = 50000,
  inner     = 5000
)
